# Abdo Debts V20 — إضافة الأصناف غير المطابقة إلى الدليل

## الجديد
- إضافة زر **+ إضافة إلى دليل الأصناف** بجانب كل صنف غير مطابق في تقرير الحركة.
- يبقى زر **مطابقة بصنف موجود** للحالات التي يكون فيها الصنف موجودًا في الدليل باسم مختلف.
- نموذج الإضافة يملأ اسم الصنف تلقائيًا من تقرير الحركة.
- يمكن إدخال كود الصنف يدويًا، أو تركه فارغًا ليولّد النظام كودًا داخليًا يبدأ بـ `MOV-`.
- عدد الفرط في العلبة إلزامي حتى يمكن حساب العلب المكافئة ومعدل البيع اليومي بشكل صحيح.
- بعد الحفظ يتم: إضافة الصنف للدليل + حفظ ربط الاسم + إعادة حساب نفس سطر الحركة فورًا.
- الربط المحفوظ يُستخدم تلقائيًا في التقارير القادمة.

## قاعدة البيانات
- لا يوجد SQL جديد لهذه النسخة؛ تعتمد على جداول V9 وV18 الموجودة مسبقًا.
