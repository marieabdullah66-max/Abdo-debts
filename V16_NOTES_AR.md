# Abdo Debts V16 — اعتماد قاعدة بيانات الأصناف

تم اعتماد ملف الأصناف المرسل بتاريخ 2026-08-30 كقاعدة أساسية لدليل الأصناف داخل البرنامج.

## نتيجة قراءة الملف
- إجمالي صفوف المصدر: **8,105**.
- الأكواد الفريدة: **8,103**.
- يوجد كودان مكرران في المصدر؛ يتم التعامل معهما بنفس منطق الاستيراد الحالي (آخر صف للكود هو المعتمد).
- **222** كودًا يبدأ بصفر؛ لذلك يتم تخزين الأكواد كـ `text` ولا يتم تحويلها إلى رقم.
- لا توجد أسماء أو أكواد أو قيم `فرط/علبة` فارغة في المصدر.

## الحقول المعتمدة
- `item_code`: كود/باركود الصنف.
- `item_name`: اسم الصنف.
- `package_form`: وحدة البيع في المصدر (`علبة` أو `فرط`).
- `units_per_box`: عدد الفرط داخل العلبة.

## طريقة التطبيق
شغّل مرة واحدة في Supabase SQL Editor:

`supabase/migrations/V16_item_catalog_seed.sql`

الملف يعمل Upsert حسب `item_code`: الموجود يتم تحديثه والجديد يتم إضافته، ولا يحذف أي صنف قديم غير موجود في الملف.

## ملاحظات للمراجعة
- الكود `8470003935876` ظهر باسمين متقاربين؛ تم اعتماد آخر اسم في المصدر: `Heliocare Ultra Gel 90 50ml`.
- الكود `915` ظهر مرتين لنفس `Reality Extra Tab`.
- الصنف `Protein A Hair Treatment 800 Ml` مسجل في المصدر بقيمة `800` فرط/علبة وتم الاحتفاظ بالقيمة كما هي بدون تخمين أو تعديل.
