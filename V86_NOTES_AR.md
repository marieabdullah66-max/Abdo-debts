# Abdo Debts V86 — المرحلة الثانية: الموظفون حسب الفرع + الأداء

هذه النسخة مبنية على V85 وتركز على جعل قسم الموظفين مناسبًا لإدارة الفرع فعليًا، مع إزالة حدود القراءة الصامتة في البيانات المالية الكبيرة.

## الموظفون أصبحوا على مستوى الفرع

- كل بطاقة موظف مرتبطة الآن بفرع محدد.
- المستخدمون الذين لديهم صلاحية `use_employee_records` ونفس الفرع يرون نفس الموظفين وسجلاتهم بدل أن تكون نسخة خاصة بكل حساب.
- أضيف فلتر فرع في صفحة الموظفين وفي تقرير PDF.
- يظهر اسم الفرع داخل بطاقة الموظف وفي تفاصيله وفي تقرير PDF.
- عند إضافة/تعديل الموظف يجب تحديد الفرع.
- الموظفون القدامى ينتقلون تلقائيًا للفرع إذا كان منشئ البطاقة مربوطًا بفرع واحد فقط.
- إذا كان منشئ البطاقة مربوطًا بأكثر من فرع، تبقى البطاقة القديمة «غير محددة» وخاصة بصاحبها حتى يفتح تعديل الموظف ويختار الفرع الصحيح. لا يتم حذف أي سجل قديم.
- سجلات الغياب والسحب والأجل والإضافي والملاحظات أصبحت مشتركة مع بطاقة الموظف داخل الفرع، مع بقاء `user_id` في السجل كتتبع لمن أنشأ الحركة.
- يمنع حذف فرع إذا كان مرتبطًا ببطاقات موظفين.

## تحسينات الأداء وصحة البيانات الطويلة المدى

- أضيف قارئ PostgREST متعدد الصفحات `sb_paged` بدل الاعتماد على `limit` كبير قد يتم قصه بصمت بواسطة Supabase.
- قوائم الفواتير والسدادات وخطط السداد والموظفين وتقاريرهم تستخدم القراءة متعددة الصفحات عند الحاجة.
- حسابات Dashboard التاريخية والفئات وAging تقرأ الصفوف على صفحات بدل سقف استجابة واحدة.
- استعلامات Dashboard الرئيسية تعمل بالتوازي لتقليل زمن الانتظار.
- تجميع أرصدة الموردين والملخصات أصبح أقل عرضة لاختفاء بيانات قديمة عند نمو القاعدة.

## مهم قبل النشر

على قاعدة V85 الحالية شغّل مرة واحدة فقط:

`supabase/migrations/V86_employee_branch_performance.sql`

ثم انشر ملفات V86. لا تشغّل `supabase/schema.sql` على قاعدة موجودة؛ الملف الكامل مخصص لمشروع Supabase جديد.
