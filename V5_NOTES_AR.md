# V5 — تصنيفات الموردين المتعددة

- إضافة قسم **تصنيفات الموردين** داخل الإعدادات للمستخدم الذي يملك صلاحية إدارة الموردين.
- إضافة / تعديل / حذف التصنيف. يمنع حذف التصنيف إذا كان مرتبطًا بموردين حتى تتم إزالة الارتباط أولًا.
- المورد الواحد يمكن ربطه بأكثر من تصنيف في نفس الوقت، مثل: **دواء + كوزمتك**.
- إضافة اختيار متعدد للتصنيفات داخل نافذة إضافة/تعديل المورد.
- عرض تصنيفات المورد في قائمة الموردين وفي صفحة تفاصيل المورد.
- إضافة فلتر **حسب التصنيف** في واجهة الموردين، ويعمل مع فلتر الفرع والبحث في نفس الوقت.
- إجمالي الدين المتبقي وعدد الموردين يتغيران حسب الفلاتر الحالية.
- تحديث PWA إلى V5 ورفع إصدار ملفات الواجهة لمنع بقاء كاش قديم على الهاتف.

## مهم قبل تشغيل V5 على قاعدة البيانات الحالية

شغّل الملف التالي **مرة واحدة فقط** من Supabase SQL Editor:

`supabase/migrations/V5_supplier_categories.sql`

الملف ينشئ جدولي التصنيفات والربط بين الموردين والتصنيفات، ولا يعدّل أو يحذف أي فواتير أو سدادات أو موردين موجودين.
