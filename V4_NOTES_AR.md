# V4 — تحديث PWA بدون كاش قديم

- إضافة رقم إصدار إلى روابط `app.js` و`styles.css` و`manifest.webmanifest` لكسر الكاش على الهاتف بعد كل نشر.
- تحديث Service Worker إلى `abdo-debts-v4`.
- تغيير استراتيجية ملفات الواجهة إلى **Network First** مع الرجوع للكاش عند انقطاع الإنترنت، بدل Cache First الذي كان قد يبقي ملفات JavaScript/CSS القديمة على الهاتف.
- تسجيل Service Worker باستخدام `updateViaCache: 'none'` وطلب فحص تحديث عند فتح التطبيق.
- حذف كاشات الإصدارات السابقة تلقائيًا عند تفعيل V4.
- الحفاظ على تعديلات V3: رصيد كل مورد، فلترة الموردين حسب الفرع، وإجمالي الدين المتبقي.
- لا يوجد أي تغيير على قاعدة البيانات ولا حاجة لإعادة تشغيل `schema.sql`.
