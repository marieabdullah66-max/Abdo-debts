# V2 — صفحة المورد المستقلة

- إضافة زر **فتح** لكل مورد في قائمة الموردين.
- زر فتح ينقل إلى صفحة مستقلة خاصة بالمورد بدل الاعتماد على نافذة الكشف فقط.
- صفحة المورد تعرض إجمالي الفواتير، إجمالي المسدد، المتبقي، والرصيد حسب الفروع.
- عرض فواتير المورد وسداداته من نفس الصفحة وفق صلاحيات المستخدم.
- إضافة زر **إضافة فاتورة** من داخل المورد مع تثبيت المورد تلقائيًا.
- إضافة زر **تسجيل سداد** من داخل المورد مع تثبيت المورد تلقائيًا ثم اختيار الفرع والفواتير.
- دعم تعديل/حذف الفواتير والسدادات من صفحة المورد حسب الصلاحيات الموجودة أصلًا.
- زر رجوع إلى قائمة الموردين مع رابط مستقل يحتوي `view=supplier&supplier_id=...`.
- تحديث Service Worker إلى `abdo-debts-v2` لتحديث ملفات الواجهة في PWA.

لا توجد تغييرات على قاعدة البيانات في V2، ولا يحتاج `schema.sql` إلى إعادة تشغيل.
