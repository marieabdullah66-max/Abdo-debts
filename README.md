# Abdo Debts — V19

برنامج مستقل لإدارة مديونيات الموردين، مبني بنفس فلسفة Abdo Supply لكن بقاعدة بيانات ومشروع منفصلين.

## الموجود حاليًا

- موردون مستقلون مع إضافة مورد سريعة أثناء إدخال الفاتورة.
- فروع ثابتة من الإعدادات، ويمكن إيقاف الفرع مع الحفاظ على بياناته.
- فاتورة: المورد، الفرع، رقم الفاتورة، القيمة، تاريخ الفاتورة، تاريخ استحقاق اختياري، ملاحظات، ومرفق PDF اختياري حتى 10MB.
- رقم الفاتورة فريد داخل المورد نفسه: لا يسمح النظام بتكرار نفس الرقم لنفس المورد حتى لو اختلف الفرع.
- سداد نقدي أو مصرفي. عند السداد المصرفي يطلب اسم المصرف فقط.
- السداد الواحد يخص موردًا واحدًا وفرعًا واحدًا، ويمكن توزيعه يدويًا على عدة فواتير داخل نفس الفرع.
- سداد جزئي مع حساب المتبقي وحالة الفاتورة تلقائيًا: غير مسددة / جزئي / مسددة.
- تعديل وحذف السداد حسب الصلاحية، مع إعادة حساب أرصدة الفواتير تلقائيًا.
- مستخدمون بصلاحيات منفصلة، ومستخدم يمكن ربطه بفرع واحد أو عدة فروع أو كل الفروع.
- Dashboard لإجمالي الفواتير، المسدد، المتبقي، المتأخر، وحالة الفواتير، وأعلى الموردين مديونية والمديونية حسب الفروع، مع Aging للرصيد المفتوح (غير مستحق / 0–30 / 31–60 / 61–90 / +90 يوم).
- صفحة الموردين تعرض Aging لكل مورد بعدد الأيام منذ أقدم فاتورة ما زال عليها رصيد، ويتبع فلتر الفرع.
- صفحة مستقلة لخطة السداد: مواعيد ومبالغ مخططة، تأجيل مع سبب، متابعة المتأخر ومستحق اليوم والأسبوع القادم، وربط الموعد بالسداد الفعلي.
- قسم مستقل لحركة الأصناف: دليل أصناف مركزي، رفع تقرير حركة `.xls/.xlsx` من منظومة البيع، تجميع المبيعات، تحويل الفرط إلى علب، وحساب معدل البيع اليومي لكل صنف حسب الفرع والفترة.
- الأسماء المختلفة بين تقرير البيع ودليل الأصناف يمكن مطابقتها مرة واحدة، ويحتفظ النظام بالمطابقة للتقارير القادمة.
- PWA مناسب للهاتف.

> اسم **Abdo Debts** مؤقت ويمكن تغييره لاحقًا بدون تغيير منطق البرنامج.

## إنشاء مشروع Supabase جديد

1. أنشئ مشروع Supabase مستقل عن Abdo Supply.
2. افتح SQL Editor وشغّل الملف `supabase/schema.sql` كاملًا.
3. انسخ `backend/.env.example` إلى `backend/.env` وضع مفاتيح المشروع الجديد:

```env
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_PUBLISHABLE_KEY=...
SUPABASE_SECRET_KEY=...
```

يستخدم الـBackend مفتاح الخدمة للعمليات المالية، بينما الواجهة لا تتصل بقاعدة البيانات مباشرة.

## إنشاء أول مدير

بعد تثبيت المتطلبات وضبط `.env`:

```bash
python scripts/create_admin.py admin "مدير النظام" "StrongPassword123"
```

بعد الدخول كمدير، أنشئ الفروع وباقي المستخدمين من الإعدادات.

## التشغيل محليًا

```bash
pip install -r requirements.txt
python run.py
```

ثم افتح `http://127.0.0.1:8000`.

## النشر

هيكل التشغيل متوافق مع نشر FastAPI المعتاد، ويوجد `app.py` في جذر المشروع كما في Abdo Supply. ضع متغيرات البيئة الثلاثة نفسها في منصة النشر.

## ملاحظات V1

- لا يوجد Audit Log حسب الاتفاق.
- التقارير المتقدمة وPDF/Excel لكشوف الحساب مؤجلة للإصدار التالي عند تحديد تفاصيلها.
- حذف فاتورة عليها سداد ممنوع؛ يجب تعديل/حذف السداد أولًا لحماية الأرصدة.
- حذف فرع مرتبط ببيانات ممنوع؛ يمكن إيقافه بدلًا من ذلك.

## V14 - Session reliability
- Automatic access-token refresh before expiry.
- Handles expired JWT responses returned as either 401 or 403.
- Deduplicates concurrent refresh requests.
- Prevents raw Supabase JWT errors from appearing in the UI.
- No database migration is required for V14.


## V15 - Searchable supplier in payment plan
- The supplier field in Add/Edit Payment Plan is now searchable by supplier name.
- The side arrow opens the full supplier list.
- No database migration is required for V15.

## V16 - قاعدة بيانات الأصناف المعتمدة
- تم اعتماد ملف الأصناف المصدّر من منظومة الصيدلية كقاعدة أساسية لدليل الأصناف.
- يحتوي المصدر على 8,105 صف و8,103 كود صنف فريد.
- يتم حفظ كود الصنف كنص للحفاظ على الأكواد التي تبدأ بصفر.
- ملف `supabase/migrations/V16_item_catalog_seed.sql` يضيف الأصناف الجديدة ويحدّث الموجودة حسب كود الصنف، ولا يحذف أصنافًا موجودة خارج الملف.
- تم استخدام وحدة البيع من ملف المصدر في حقل دليل الأصناف، مع عدد الفرط داخل العلبة كما هو في المصدر.


## V17
تم تحسين أداء دليل الأصناف: تحميل 100 صنف افتراضيًا، بحث على كامل قاعدة البيانات، وزر إظهار الكل عند الحاجة. لا يوجد SQL جديد لهذه النسخة.


## V18 — تحليل حركة الأصناف
- شغّل `supabase/migrations/V18_item_movement_analysis.sql` مرة واحدة إذا كنت تحدّث من V17.
- يدعم تقرير `.xls` المصدّر مباشرة من منظومة البيع بدون تعديل يدوي.


## V19 — استبدال دليل الأصناف
- يمكن حذف دليل الأصناف الحالي بالكامل من صفحة **دليل الأصناف** ثم استيراد دليل جديد في أي وقت.
- الحذف يتطلب كتابة عبارة `حذف الدليل` للتأكيد لتجنب الحذف بالخطأ.
- تقارير حركة الأصناف القديمة لا تُحذف؛ يتم فقط إلغاء روابطها ومطابقاتها مع الدليل القديم.
- مبيعات العلبة فقط تبقى قابلة للحساب، أما الصفوف التي تحتوي مبيعات فرط فتحتاج مطابقة/إعادة استيراد بعد إضافة الدليل الجديد.
- استيراد دليل الأصناف أصبح يدعم `.xls` و`.xlsx`.
- شغّل `supabase/migrations/V19_item_catalog_replace.sql` مرة واحدة عند التحديث من V18.
