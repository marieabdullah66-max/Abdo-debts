# Abdo Debts V9 — حركة الأصناف

تمت إضافة قسم مستقل باسم **حركة الأصناف** لا يرتبط بالموردين أو الفواتير أو السدادات.

## الموجود الآن
- صفحة مستقلة لحركة الأصناف.
- تبويب **دليل الأصناف**.
- إضافة وتعديل وحذف صنف يدويًا.
- بحث بالكود أو اسم الصنف.
- بيانات الصنف: الكود، الاسم، شكل التعبئة، وعدد الفرط في العلبة.
- استيراد دليل الأصناف من Excel بصيغة `.xlsx`.
- معاينة أولية للملف واختيار ورقة Excel وصف العناوين وربط الأعمدة يدويًا.
- الاستيراد يعتمد على كود الصنف: الموجود يتم تحديثه والجديد تتم إضافته.
- تبويب **تحليل الحركة** مجهز كواجهة مستقلة، لكن ربط تقرير الحركة الشهري مؤجل إلى حين مراجعة تقرير فعلي.
- صلاحيتان مستقلتان: `view_item_analysis` و `manage_item_catalog`.

## قاعدة البيانات
قبل تشغيل الميزة على النسخة المنشورة، نفّذ ملف:

`supabase/migrations/V9_item_catalog.sql`

داخل Supabase SQL Editor.

## حساب تحليل الحركة المتفق عليه للمرحلة القادمة
- العلب المكافئة = العلب المباعة + (الفرط المباع ÷ عدد الفرط في العلبة)
- معدل البيع اليومي = العلب المكافئة ÷ عدد أيام الشهر

لن يتم تثبيت قراءة تقرير الحركة الشهري قبل مراجعة ملف Excel حقيقي من المنظومة.
