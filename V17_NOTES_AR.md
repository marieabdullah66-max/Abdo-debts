# V17 — تسريع دليل الأصناف

- عند فتح دليل الأصناف يتم تحميل أول 100 صنف فقط بدل تحميل القاعدة كاملة.
- البحث بالكود أو الاسم يتم على قاعدة البيانات كاملة من الخادم، وليس فقط على الـ100 صنف الظاهرين.
- إضافة تأخير قصير للبحث (debounce) لتقليل الطلبات أثناء الكتابة وتسريع الواجهة.
- إضافة زر **إظهار الكل** لتحميل كل الأصناف فقط عند الطلب.
- بعد إظهار الكل يظهر زر **عرض 100 فقط** للعودة للوضع السريع.
- عرض عداد واضح مثل: `عرض 100 من 8,103 صنف` أو عدد نتائج البحث.
- لا يحتاج هذا التعديل إلى SQL جديد.
